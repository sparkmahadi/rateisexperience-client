
import Spinner from '../Spinner/Spinner';

const Register = () => {

    return (
        <div>
            <h3 className='text-xl text-error font-semibold text-center'>Sorry. You are not allowed to Sign Up!! Contact Admin!</h3>
        </div>
    );
};

export default Register;